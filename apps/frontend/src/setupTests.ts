// Vitest setup file
// You can add global setup options here if needed in the future.

// For example, extending expect:
// import { expect } from 'vitest';
// import matchers from '@testing-library/jest-dom/matchers';
// expect.extend(matchers);
