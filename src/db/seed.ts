const now = new Date();

const seedData = [
  {
    createdAt: now,
    updatedAt: now,
  },
  {
    createdAt: now,
    updatedAt: now,
  },
  {
    createdAt: now,
    updatedAt: now,
  },
];

export default seedData;
